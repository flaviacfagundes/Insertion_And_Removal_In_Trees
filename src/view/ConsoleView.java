
package view;

import java.util.HashMap;

public class ConsoleView {
    public ConsoleView(String title, int x, int y) {
        setup(title, x, y);
    }
    public void setup(String title, int x, int y) {
        HashMap<String, Integer> size = new HashMap<>();
    }
}
